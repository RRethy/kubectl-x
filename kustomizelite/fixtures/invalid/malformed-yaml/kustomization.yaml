apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
    - service.yaml  # Invalid indentation

commonLabels:
  app: my-app
  environment: production
    version: "1.0.0"  # Invalid indentation