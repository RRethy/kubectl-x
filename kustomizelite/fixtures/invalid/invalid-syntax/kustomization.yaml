apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources: [
  deployment.yaml
  service.yaml  # Missing comma in array
]

commonLabels:
  app: my-app
  environment: {{ .Environment }}  # Invalid template syntax